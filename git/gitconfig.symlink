[core]
    eol = lf
    autocrlf = true
    filemode = false
[credential]
    helper = cache --timeout=846000
[push]
    default = tracking

# include data not shared in my dotfiles
[include]
    path = ~/.gitconfig.include
